{
  "name": "aurora-ds",
  "private": true,
  "version": "0.0.1",
  "description": "Aurora Design System — accessible, themeable React component library",
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "test:a11y": "turbo run test:a11y",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "tokens:build": "pnpm --filter @aurora-ds/tokens build",
    "storybook": "pnpm --filter @aurora-ds/storybook dev",
    "docs": "pnpm --filter @aurora-ds/docs dev",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^1.13.0",
    "typescript": "^5.3.0",
    "prettier": "^3.2.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "tooling/*"
  ]
}
